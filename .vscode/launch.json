{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Bot",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/src/bot_main.ts",
      "preLaunchTask": "tsc: build - tsconfig.json",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "console": "integratedTerminal"
    }
  ]
}
